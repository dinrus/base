module col.model.IContainer;

/*******************************************************************************

        Генерный контейнер
        
*******************************************************************************/

interface ИКонтейнер (З)
{
        т_мера размер ();

        бул пуст_ли ();

        ИКонтейнер dup ();

        ИКонтейнер очисть ();                          

        ИКонтейнер сбрось ();                          

        ИКонтейнер проверь ();

        бул содержит (З значение);

        бул возьми (ref З элемент);

        З[] вМассив (З[] приёмн = пусто);

        т_мера удали (З элемент, бул все);

        цел opApply (цел delegate(ref З значение) дг);

        т_мера замени (З старЭлемент, З новЭлемент, бул все);
}


/*******************************************************************************

        Функция сравнения
        
*******************************************************************************/

template Сравни (З)
{
        alias цел function (ref З a, ref З b) Сравни;
}

