module util.worktools;
public import stdrus, stdrusex;

	цел компилируйПакет(ткст путь, ткст англИмяСтатБиб = "myLib");
	цел удалиФайлы(ткст путь, ткст флрасш);
	цел импортируйБиблиотеки( ткст откуда =".");
	проц зачисть(ткст путь = ".");	
