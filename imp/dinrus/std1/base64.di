module std.base64;
public import stdrus:  кодируй64,  раскодируй64;

alias кодируй64 encode;
alias раскодируй64 decode;


 class База64
{
	this();
	сим[] вТкст(сим[] ткт);
	сим[] изТкст(сим[] ткт);
    сим[] кодируй(сим[] ткт);
	сим[] раскодируй(сим[] иткт);
}