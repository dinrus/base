module net.InternetAddress;

private import net.device.Berkeley;

/*******************************************************************************


*******************************************************************************/

export class АдресИнтернета : АдресИПв4
{
        /***********************************************************************

                useful for Datagrams

        ***********************************************************************/

        export this(){}

        /***********************************************************************

                -порт- can be ПОРТ_ЛЮБОЙ
                -адр- is an ИП адрес or хост имя

        ***********************************************************************/

        export this (ткст адр, цел порт = ПОРТ_ЛЮБОЙ)
        {
                foreach (цел i, сим c; адр)
                         if (c is ':')
                            {
                            порт = разбор (адр [i+1 .. $]);
                            адр = адр [0 .. i];
                            break;
                            }

                super (адр, cast(бкрат) порт);
        }

        /***********************************************************************


        ***********************************************************************/

        export this (бцел адр, бкрат порт)
        {
                super (адр, порт);
        }


        /***********************************************************************


        ***********************************************************************/

        export this (бкрат порт)
        {
                super (порт);
        }

        /**********************************************************************

        **********************************************************************/

        private static цел разбор (ткст s)
        {       
                цел число;

                foreach (c; s)
                         число = число * 10 + (c - '0');

                return число;
        }
}
