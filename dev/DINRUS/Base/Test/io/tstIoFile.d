        import io.File;

        проц main()
        {
                auto x = new Файл ("");

                auto контент = cast(ткст) Файл("tstIoFile.d").читай;
               // Стдвыв (контент).нс;
			   скажинс(контент);			   
        }