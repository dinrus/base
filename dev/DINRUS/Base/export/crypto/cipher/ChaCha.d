module crypto.cipher.ChaCha;

private import crypto.cipher.Cipher;
private import crypto.cipher.Salsa20;

/** Реализация ЧаЧа (ChaCha), разработанная Daniel J. Bernstein. */
extern(D) class ЧаЧа : Сальса20
{
    ткст имя();    
    this();
  //  protected проц установиКлюч();    
  //  protected проц установиIV();    
   // protected проц словоСальса20ВБбайт(бцел[] ввод, ref ббайт[] вывод);
}
